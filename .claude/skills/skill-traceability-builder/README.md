# 🎯 SKILL-TRACEABILITY-BUILDER (Atomic — Tier 2)

> Atomic AI skill chuyên build RTM (Requirements Traceability Matrix)
> + detect orphans + version drift.

## 📋 Vị trí

```
Tier 1: babok-knowledge/artifacts/ART-RTM.yaml
              ↓ references
Tier 2: ba-agent/skills/SKILL-TRACEABILITY-BUILDER/   ← bạn đang ở đây
              ↓ delegated by
Tier 3: generated/babok/ba-doc-write (RTM section in BRD)
        generated/babok/ba-doc-review (traceability check)
```

## 📁 Cấu trúc

```text
SKILL-TRACEABILITY-BUILDER/
├── card.yaml
├── SKILL.md
├── README.md
├── resources/
│   └── art_rtm_ref.md
└── examples/
    └── example_rtm_build.md
```

## 🔌 Input/Output

**Input:**
- `business_requirements[]`
- `functional_requirements[]` (with source_br field)
- `user_stories[]` (with implements_fr field)
- `test_cases[]` (with tests_story field)
- `implementation_refs[]` (optional)

**Output:** JSON với:
- `rtm_summary` — counts + coverage %
- `rtm_rows` — matrix structure
- `orphans_detected` — flagged orphans
- `coverage_gaps` — missing test cases
- `stale_links` — version drift warnings

## 🔗 Skill chain

```
SKILL-USER-STORY-GENERATOR (upstream — provides stories)
SKILL-AC-GENERATOR (upstream — provides AC)
SKILL-BR-EXTRACTOR (upstream — provides business rules)
   ↓
SKILL-TRACEABILITY-BUILDER (← THIS — final RTM)
   ↓
SKILL-IMPACT-ANALYZER (roadmap)
```

## 🛠 Validators

- VALIDATOR-END-TO-END-COVERAGE
- VALIDATOR-NO-ORPHANS

## 📚 References

- Spec: [`card.yaml`](./card.yaml)
- Tier 1: `f:/skill-generator/babok-knowledge/artifacts/ART-RTM.yaml`
- Task: `f:/skill-generator/babok-knowledge/task/TASK-LIFECYCLE-001.yaml`

## 📦 Generated by

Skill Creator Ultra v1.0 — Phase 2
