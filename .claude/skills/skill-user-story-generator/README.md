# 🎯 SKILL-USER-STORY-GENERATOR (Atomic — Tier 2)

> Atomic AI skill chuyên sinh User Story chuẩn IIBA/INVEST từ structured
> requirements. **Không phải skill user-facing** — được delegated từ
> composite skill `ba-doc-write` (Tier 3).

## 📋 Vị trí trong kiến trúc 3-tier

```
Tier 1: babok-knowledge/  → ART-USER-STORY.yaml (data spec)
                              ↓ references
Tier 2: ba-agent/skills/  → THIS SKILL (atomic execution) ← bạn đang ở đây
                              ↓ delegated by
Tier 3: generated/babok/  → ba-doc-write (composite UX)
```

## 📁 Cấu trúc

```text
SKILL-USER-STORY-GENERATOR/
├── card.yaml                       ← Original spec (skill.schema.yaml)
├── SKILL.md                        ← Claude Code executable
├── README.md                       ← File này
├── resources/
│   ├── art_user_story_ref.md       ← Extracted from babok-knowledge
│   ├── tech_user_story_ref.md      ← Extracted from babok-knowledge
│   └── invest_validator_ref.md     ← Extracted from ba-agent/validators
└── examples/
    ├── example_happy.md            ← 3pt story generation
    └── example_split.md            ← 13pt → auto-split into 3 stories
```

## ⚙️ Khi nào skill được trigger

### Direct (advanced usage)
```
User: "Sinh user story atomic từ structured BR-019..."
→ skill-user-story-generator activates trực tiếp
```

### Via composite (default — recommended)
```
User: "Viết user story cho wishlist"
→ ba-doc-write (composite Tier 3)
   → interview BACCM (gather context)
   → structure input
   → DELEGATE → skill-user-story-generator (this skill)
   → receive JSON
   → render markdown for user
```

## 🔌 Input/Output Contract

**Input** (per `card.yaml.inputs`):
```yaml
business_requirements: [BR-XXX, ...]
stakeholder_requirements: [persona, pain, outcome]
business_rules: [optional]
context: {domain, sprint, epic}
```

**Output** (JSON):
```json
{
  "skill": "SKILL-USER-STORY-GENERATOR",
  "status": "success | auto_split_applied | error",
  "artifact": { /* User Story object per ART-USER-STORY.yaml */ },
  "validators_passed": [...],
  "followup_skills_recommended": [...],
  "human_review_areas": [...]
}
```

## 🔗 Skill chain (per ba-agent/orchestration/skill_flow.yaml)

```
SKILL-REQUIREMENT-EXTRACTOR    (upstream — extracts from text)
   ↓
SKILL-USER-STORY-GENERATOR     (← THIS)
   ↓
SKILL-AC-GENERATOR             (downstream — deeper AC)
   ↓
SKILL-TRACEABILITY-BUILDER     (downstream — link to BR/tests)
```

## 🛠 Validators applied

- **VALIDATOR-INVEST** — 6 criteria, auto-fix `split_story` if oversized
- **VALIDATOR-ACTOR-DEFINITION** — actor specific (not "user")
- **VALIDATOR-TESTABILITY** — AC writable as test cases

## 📚 References

- Spec card: [`card.yaml`](./card.yaml)
- Tier 1 artifact: `f:/skill-generator/babok-knowledge/artifacts/ART-USER-STORY.yaml`
- Tier 1 technique: `f:/skill-generator/babok-knowledge/technique/TECH-USER-STORY.yaml`
- Tier 1 task: `f:/skill-generator/babok-knowledge/task/TASK-RADD-001.yaml`
- Tier 2 validator: `f:/skill-generator/ba-agent/validators/VALIDATOR-INVEST.yaml`
- Tier 2 workflow: `f:/skill-generator/ba-agent/orchestration/workflows/WF-RADD.yaml`

## ⚠️ Nguyên tắc atomic skill

| ✅ DO | ❌ DON'T |
|---|---|
| Return structured JSON | Return markdown for human |
| Apply validators bắt buộc | Bypass validators |
| Suggest followup skills | Force-trigger downstream |
| Auto-fix khi safe (split) | Auto-fix decisions PO cần approve |
| Reference Tier 1 schemas | Hardcode validation logic |

## 📦 Generated by

Skill Creator Ultra v1.0 — Phase 1 Sample (architecture proof)
