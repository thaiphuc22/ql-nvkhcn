# 🎯 SKILL-REQUIREMENT-EXTRACTOR (Atomic — Tier 2)

> Atomic AI skill chuyên extract structured requirements từ
> unstructured stakeholder communication (transcripts, notes, emails).

## ⚠️ Tier 1 Gaps Detected

Các Tier 1 references trong `card.yaml` chưa tồn tại:
- `babok-knowledge/technique/TECH-INTERVIEW.yaml`
- `babok-knowledge/technique/TECH-WORKSHOP.yaml`
- `babok-knowledge/artifacts/ART-INITIAL-REQUIREMENTS.yaml`
- `babok-knowledge/artifacts/ART-ELICITATION-NOTES.yaml`

→ Skill dùng `resources/elicitation_patterns.md` làm fallback (extracted
patterns từ BABOK v3 §10).

→ **Action:** Cần update babok-knowledge maintainer tạo 4 files trên.

## 📋 Vị trí

```
Tier 1: babok-knowledge/knowledge_areas/KA-ELICITATION.yaml ✅
        + 4 file refs MISSING (see above)
              ↓ references (with gaps)
Tier 2: ba-agent/skills/SKILL-REQUIREMENT-EXTRACTOR/   ← bạn đang ở đây
              ↓ delegated by
Tier 3: generated/babok/ba-doc-write
        (workflow WF-DISCOVERY)
```

## 📁 Cấu trúc

```text
SKILL-REQUIREMENT-EXTRACTOR/
├── card.yaml                           ← spec (most complete in Tier 2)
├── SKILL.md                            ← executable
├── README.md
├── resources/
│   └── elicitation_patterns.md         ← Tier 1 fallback patterns
└── examples/
    └── example_req_extraction.md
```

## 🔌 Input/Output

**Input:**
- `transcript` (text) — meeting/interview/workshop output (REQUIRED)
- `notes` (text) — supplementary
- `stakeholder_context` (object) — speaker roles, influence, interest

**Output:** JSON với:
- `requirements[]` — classified với confidence + source citation
- `assumptions[]`
- `open_questions[]`
- `ambiguities[]`

## 🔗 Skill chain

```
[meeting transcript / notes / emails]
   ↓
SKILL-REQUIREMENT-EXTRACTOR (← THIS — entry to elicitation phase)
   ↓
├─ SKILL-USER-STORY-GENERATOR  (Functional reqs → stories)
├─ SKILL-BR-EXTRACTOR           (Constraint → business rules)
└─ SKILL-CONFLICT-DETECTOR      (Ambiguities → flagged conflicts) [roadmap]
```

## 🛠 Validators

- VALIDATOR-REQUIREMENT-ATOMICITY
- VALIDATOR-NO-DUPLICATION
- VALIDATOR-BUSINESS-VALUE

## 🧠 Memory Interactions (per card.yaml)

```yaml
reads:
  - stakeholder_history    # Detect speaker contradictions
  - domain_ontology         # Normalize terms
```

When `ba-agent/memory/` runtime available, skill query memory để adjust
confidence + cross-reference.

## 📚 References

- Spec: [`card.yaml`](./card.yaml)
- Tier 1 KA: `f:/skill-generator/babok-knowledge/knowledge_areas/KA-ELICITATION.yaml`
- Tier 1 task: `f:/skill-generator/babok-knowledge/task/TASK-ELICIT-001.yaml`
- Local fallback: [`resources/elicitation_patterns.md`](./resources/elicitation_patterns.md)

## 📦 Generated by

Skill Creator Ultra v1.0 — Phase 2
